# =============================================================================
# Environment Variables for ASVspoof 5 Domain-Invariant CM
# Copy this file to .env and customize for your environment
# =============================================================================

# Path to ASVspoof 5 dataset (download from https://zenodo.org/records/10511532)
ASVSPOOF5_ROOT=/scratch-shared/myuser/asvspoof5

# Wandb API key for experiment tracking (get from https://wandb.ai/authorize)
WANDB_API_KEY=your_api_key_here

# Wandb project name
WANDB_PROJECT=asvspoof5-dann

# HuggingFace cache directory (for SSL model downloads)
HF_HOME=/scratch-shared/myuser/.cache/huggingface

# Pre-computed codec augmentation cache (speeds up DANN training ~5x)
# Generate with: python scripts/precompute_augmentations.py
# Leave unset for on-the-fly augmentation (slower but no disk overhead)
AUGMENTATION_CACHE_DIR=/scratch-shared/myuser/asvspoof5_augmented_cache

# Runs/checkpoints directory (default: ./runs in project root)
# Recommended: use persistent storage on HPC, not scratch
RUNS_DIR=/projects/prjs1904/runs

# Optional: Disable wandb (set to "disabled" to skip logging)
# WANDB_MODE=disabled
